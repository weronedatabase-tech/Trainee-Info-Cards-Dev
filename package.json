{
  "name": "trainee-info-cards",
  "version": "1.0.0",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build": "mkdir -p dist && cp -r index.html backend frontend assets manifest.json sw.js dist/"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
